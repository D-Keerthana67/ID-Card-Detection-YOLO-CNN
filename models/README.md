# Models

Place your trained model here:

- `id_card_yolo.pt` — custom YOLO model trained to detect ID cards.

Model weights are ignored by Git. Do not upload private or restricted training data or model files to a public repository without permission.
